# WeatherTech OS Module Registry

This registry records the major modules currently present in the repository. Status values describe the current implementation from repository inspection, not a promise of production completeness.

## Status Legend

- Complete: implemented, integrated, and covered by current validation expectations.
- Mostly Complete: substantial workflow exists, with remaining integration or polish work.
- Partially Complete: meaningful implementation exists, but daily production coverage is incomplete.
- Foundation Only: architecture or UI foundation exists, but live workflow capability is intentionally limited.
- Missing: not currently implemented as a working module.

## Modules

| Module | Purpose | Current Status | Owner | Dependencies | Major Components | Related Routes | Completed | Planned Future Work |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Dashboard | Owner command center for daily operations, KPIs, priorities, and company context. | Mostly Complete | WeatherTech OS internal operations | `CrmSnapshot`, metrics, company scope, communications, jobs, estimates, invoices | `DashboardView`, dashboard metrics helpers, company switcher | Root app `/` with `dashboard` workspace view | Executive dashboard and dark-mode hierarchy are implemented. | Continue business-value refinements only when approved; avoid duplicating module-level detail. |
| CRM | Shared operating workspace for inbox, leads, customers, estimates, scopes, jobs, and follow-up activity. | Mostly Complete | WeatherTech OS internal operations | Supabase CRM tables, `lib/crm/repository.ts`, `CrmSnapshot` | `CrmApp`, `LeadsView`, `CustomersView`, `UnifiedInboxView`, customer workspace sections | Root app `/`; API lead intake routes | Core CRM hardening and CRM Identity Integrity Phase 1 are complete, pushed, deployed, and production-schema validated. | Continue reliability, permissions hardening, and workflow-specific testing; production reconciliation remains an explicit owner-reviewed operation. |
| Customer 360 | Customer-centered workspace combining profile, relationships, timeline, actions, and related records. | Mostly Complete | Office staff and sales workflow | Customers, leads, estimates, jobs, inspections, communications, documents, photos, reconciliation and accountability ledgers | Customer workspace, Identity Reconciliation, and repeat-opportunity panels inside `CrmApp` | Root app `/` with `customers` workspace view | Customer 360 includes a company-partitioned reconciliation queue and an explicit same-company Create repeat opportunity workflow with independent first-touch accountability, transaction boundaries, and retry convergence. | No automatic backfill, bulk merge, or inferred repeat opportunity is authorized; a production reconciliation still requires one exact owner-selected graph. |
| Leads | Manage lead records, first-touch attribution, assigned ownership, funnel milestones, estimates, follow-up, and reviewed reconciliation to customers/jobs. | Mostly Complete | Sales and office staff | `leads`, `lead_accountability`, immutable accountability events, companies, campaigns, customers, lead intake and identity-reconciliation helpers | `LeadsView`, accountability panels, lead intake API routes, `lib/crm/leadIntake.ts`, `lib/crm/marketingAccountability.ts`, `lib/crm/identityReconciliation.ts` | Root app `/` with `leads`; provider intake routes and `/api/leads/intake/retry` | Lead Attribution Phase 1 adds locked deterministic first-touch or explicit unknown/review state, explicit owner assignment, human first response, linked funnel milestones, structured won/lost outcomes, and company-isolated immutable evidence. Existing Production test leads were not backfilled. | Production has no real Phase 1 accountability evidence yet. Official Mighty Apes testing and other provider activation remain separate; later communications may not overwrite locked first touch. |
| Customers | Manage customer records, open Customer 360, and explicitly review customer/property reconciliation. | Mostly Complete | Office staff, with owner/admin reconciliation approval | `customers`, related leads, properties, estimates, inspections, jobs, schedules, office tasks, reconciliation audit ledger | `CustomersView`, Customer 360 and Identity Reconciliation panels | Root app `/` with `customers` workspace view | Customer creation/editing and Customer 360 foundations are implemented; reviewed reconciliation is deployed and production-schema validated without changing a production graph. | Continue ordinary workflow hardening when approved; production reconciliation requires an exact owner-selected graph and may not be automated or inferred. |
| Estimates | Build, edit, approve, create customer-safe proposals, draft deposits, and hand off estimates to jobs. | Mostly Complete | Sales and estimating | `estimates`, `estimate_line_items`, proposal revisions/sections/options, customers, leads, jobs, signatures, documents, invoices | `EstimatesView`, Proposal Builder 2.0 panel, `lib/crm/proposals.ts`, native proposal-signing routes, estimate templates, repository estimate functions | Root app `/` with `estimates` workspace view; `/proposal/sign/[requestId]` | Proposal-to-Sold Job Operational Completion Phase 1 is Production-deployed: a finalized immutable revision and deterministic customer-safe PDF can be delivered through the native company-scoped signing boundary, the selected option and optional deposit requirement are fixed at acceptance, deposit-required conversion remains gated until the exact deposit is recorded, and retry-safe conversion creates exactly one sold job. | Preserve the requirement that the electronic-record/customer disclosure receive legal review before the first real customer electronic-signature delivery. Do not treat that legal-review requirement as permission to invent or rewrite legal language. DocuSign/Dropbox Sign delivery, IHC payment activation, and QuickBooks accounting export remain separately gated. |
| Scopes | Generate and manage scopes of work and reusable templates. | Partially Complete | Sales and estimating | `scopes`, `scope_templates`, estimates, company/trade templates | `ScopeGeneratorView`, `scopeTemplates.ts`, `estimateTemplates.ts` | Root app `/` with `scopes` workspace view | Roofing and painting template foundations exist. | Continue template governance and AI-assisted drafting when approved. |
| Operations | Office Operations Command Center for operational priority queues and quick navigation. | Mostly Complete | Office operations | Jobs, schedule events, estimates, signatures, inspections, communications, materials | `OfficeOperationsView`, operations helper logic | Root app `/` with `operations` workspace view | Office operations command center and hardening are complete. | Expand only with existing-module queues and validated workflow needs. |
| Dispatch | Crew scheduling and dispatch-oriented job calendar workspace. | Mostly Complete | Production coordination | Jobs, schedule events, employees, assignments, inspections | Dispatch workspace inside Jobs/Production area, schedule helpers | Root app `/` with `jobs` and `calendar` workspace views | Dispatch and crew scheduling foundation exists. | Add route optimization or calendar sync only in approved integration sprints. |
| Production | Job production board, field workflow, checklists, materials, logs, and production summary. | Mostly Complete | Production team | Jobs, accepted proposals, job tasks, notes, materials, photos, daily logs, assignments | Job production panels inside `JobsView`, proposal-to-sold conversion RPC, production helper functions | Root app `/` with `jobs` workspace view | Field production mobile workflow and production board are implemented. Accepted native proposals now convert through a company-isolated, retry-convergent boundary that returns the same source-linked sold job instead of creating duplicates. | Continue field usability, document linkage, and permissions hardening. A real sold job must arise from an authorized business acceptance, never from Production validation. |
| Inspections | Schedule, perform, document, and convert inspections into estimates or optional reports. | Mostly Complete | Sales and field estimators | `inspections`, jobs, customers, estimates, documents, photos | `InspectionsView`, inspection tabs and workflow helpers | Root app `/` with `inspections` workspace view | Inspections workflow and safe cancel/restore are implemented. | Continue report polish and mobile roof-walk workflow only when approved. |
| Communications | Unified inbox and communications readiness across calls, SMS, email, website, Google Business Profile, Yelp, GHL, QuickBooks Online sync logs, DocuSign/Dropbox Sign sync logs, calendar, and internal notes. | Partially Complete | Office staff and integrations | `email_messages`, `sms_messages`, `integration_sync_logs`, communications helpers, provider readiness | `UnifiedInboxView`, communications helpers, provider cards | Root app `/` with `inbox`; related provider API routes | Communications Hub is implemented; Gmail, Google Calendar, and GoHighLevel have verified connection checkpoints. The inbound-only Twilio implementation is authenticated, exactly routed, idempotent, inbox-backed, and outbound-locked. WeatherTech Tucson ending `3145` and WeatherTech Phoenix ending `1326` are live-validated; IHC ending `6930` is mapped and active at `ready_for_live_test`. | IHC still needs a separate controlled live inbound validation. Twilio has no completed A2P Brand/Campaign; outbound SMS, voice routing, MMS processing, auto-replies, reminders, and campaigns remain unavailable. |
| Documents | Document and signature workspace for estimates, proposals, scopes, invoices, reports, warranties, and related records. | Partially Complete | Office staff | `documents`, proposal records, estimates, scopes, jobs, inspections, signatures, electronic signature foundation helpers | `DocumentsAndSignaturesView`, `lib/crm/documents.ts`, `lib/crm/proposals.ts`, `lib/crm/electronicSignatureFoundation.ts`, native proposal signing/exchange routes | Root app `/` with `documents` workspace view; `/proposal/sign/[requestId]` | Document templates, the Document Center, deterministic native proposal and receipt PDFs, proposal/signed-proposal categories, private customer documents, native invitation exchange, acceptance/signature evidence, and an exact immutable signed receipt are implemented. A consumed signed invitation can mint only a bounded terminal read-only receipt session; it cannot regain acceptance authority. | No real customer delivery was used for validation. Additional document workflows and DocuSign/Dropbox Sign delivery remain separately gated, and the native electronic-record/customer disclosure remains subject to legal review before first real customer delivery. |
| Website & Marketing | Company-scoped acquisition evidence, campaigns, manual monthly spend, accountability reporting, and provider-intake foundations. | Mostly Complete | Owner/admin and marketing operations | Unified Lead Intake Hub, `marketing_campaigns`, `marketing_spend_months`, lead accountability, provider routes, source mappings | `WebsiteMarketingView`, `MarketingAccountabilityWorkspace`, `lib/crm/marketingAccountability.ts`, provider intake helpers | Root app `/` with Website & Marketing; website, Google Business Profile, Yelp, Mighty Apes, and retry intake routes | Phase 1 implements owner/admin campaign and spend entry plus lead count, spend, CPL, booking, inspection, closing, cost-per-sale, attributed revenue, revenue/spend, contact/follow-up queues, coverage, and data-quality reporting by company, Phoenix month, and source. Provider foundations remain disabled or separately gated. | Real spend and real accountable leads must be entered/received before KPIs are business truth. Estimates without explicit follow-up dates remain a gap; no SLA was invented. Provider campaign auto-registration, campaign/spend deletion, provider activation, and outbound messaging remain outside Phase 1. |
| Customer Portal | Customer-facing portal workspace boundary for project status, proposal visibility, documents, payments summary, and photos. | Foundation Only | Customer experience | Customers, jobs, estimates, proposal records, documents, invoices, photos | Customer portal workspace inside `CrmApp` | Root app `/` with `customerPortal` workspace view | Portal navigation entry, proposal summary, and proposal payment schedule visibility exist. | Live customer access, online payments, live proposal acceptance, and strict portal isolation require approved security and activation work. |
| Employee Portal | Employee-facing portal workspace boundary for field and assignment concepts. | Foundation Only | Field operations | Employees, assignments, jobs, time entries, inspections | Employee portal workspace inside `CrmApp` | Root app `/` with `employeePortal` workspace view | Portal navigation entry and foundation exist. | Live employee role flows and permissions require approved security work. |
| Settings | Platform configuration view for company workflow settings, Supabase status, and administration context. | Partially Complete | Admin users | Companies, workflow settings, auth/session state | `SettingsView` | Root app `/` with `settings` workspace view | Settings view exists. | Permissions and configurable workflows need approved future work. |
| Integrations | Provider readiness and company-scoped connection workflows without fake connectivity. | Partially Complete | Integration owner | `integration_connections`, `integration_sync_logs`, provider registries | `IntegrationsView`, provider-specific server routes and helpers | Root app `/` with `integrations`; provider API routes | Production Supabase, document storage, Gmail, Google Calendar, GoHighLevel, WeatherTech-only Stripe, inbound-only Twilio, and the Mighty Apes Yelp receiver have deployed production checkpoints. Twilio readiness is evidence-based per exact route; Mighty Apes implementation/schema/deployment are complete but its official provider test has not run. The native signing release did not activate or claim DocuSign/Dropbox Sign connectivity. | Provider health must remain truthful. Reverify server-side Mighty Apes configuration and run Send Test Delivery only under separate authorization before claiming provider validation. Twilio follow-up, QuickBooks, Google Business Profile, DocuSign/Dropbox Sign, IHC Stripe, direct Yelp API/OAuth, and other providers remain separately gated. |
| Production Readiness | Deployment, environment, migration, provider, company-mapping, regression, monitoring, backup, launch-gate, and controlled-test readiness. | Partially Complete | Owner/admin launch workflow | CRM snapshot, Integration Center providers, provider setup docs, sprint validation process, readiness endpoints | `ProductionReadinessView`, `lib/crm/productionReadiness.ts`, `lib/deployment/stagingReadiness.ts`, `docs/PRODUCTION_ACTIVATION_READINESS.md` | Root app `/` with `productionReadiness`; `/api/health`; `/api/readiness` | WeatherTech OS is deployed to Vercel at its canonical HTTPS URL; Production Supabase matches all `51/51` committed migrations through Proposal-to-Sold Job Operational Completion Phase 1. Merge/deployment SHA `7186001eec28177a32b454168e5fd05b43af9937` passed repository, isolated lifecycle, health/security, database, and read-only Browser gates without a Production business write. | A healthy deployment is not final activation: readiness remains truthfully HTTP 503 while Twilio inbound, Gmail send, and Google Calendar write are enabled and broad `WTOS_PRODUCTION_APPROVED` remains false. Legal review, DocuSign/Dropbox Sign activation, Mighty Apes official testing, monitoring, and backup evidence remain distinct gates. |
| Calendar | Schedule overview and event management for inspections, estimates, jobs, follow-ups, and material delivery. | Mostly Complete | Office operations | `schedule_events`, jobs, integrations calendar helpers | `CalendarView`, schedule repository functions | Root app `/` with `calendar` workspace view | Calendar and scheduling are implemented, and Google Calendar has a verified production connection checkpoint. | Current connector health and the enabled calendar-write gate require owner review before broader operational use. |
| Photos | Jobsite photo management using existing Supabase storage capability. | Partially Complete | Field operations | `job_photos`, Supabase storage, jobs, inspections | `PhotosView`, repository photo upload helpers | Root app `/` with `photos` workspace view | Native photo foundation exists. | Customer-visible photo separation and advanced document sets require approved work. |
| Materials | Material order and readiness tracking. | Partially Complete | Production coordination | `material_orders`, `material_order_items`, jobs | `MaterialOrdersView`, operations calculations | Root app `/` with `orders` workspace view | Material order management exists. | Supplier integrations and inventory workflows are future work. |
| Routes | Route planning foundation for jobs and leads. | Foundation Only | Dispatch | `route_plans`, `route_plan_stops`, Google Maps helpers | `RoutesView`, `lib/crm/integrations.ts` route helpers | Root app `/` with `routes` workspace view | Google Maps routing foundation exists. | Live Google Maps API integration requires configuration. |
| Invoices | Invoice records, line items, proposal deposit drafts, payments, financial summaries, WeatherTech Stripe collection, and disabled-by-default QuickBooks mapping readiness. | Partially Complete | Office/accounting | `invoices`, `invoice_line_items`, `payments`, Stripe mappings, estimates, accepted proposal records, jobs | `InvoicesView`, proposal deposit gate, `StripeInvoicePayment`, `StripeInvoiceRefund`, invoice calculation helpers | Root app `/` with `invoices`; Stripe payment/refund/webhook API routes | The owner-gated WeatherTech Payment Element, webhook accounting, atomic refund reconciliation, and company isolation are implemented and production-validated. Proposal conversion now enforces the accepted revision's deposit requirement and exactly identifies the qualifying deposit without charging or collecting funds itself. | QuickBooks accounting writes and IHC payments remain disabled pending separate authorization and account configuration. A real deposit must never be created or collected solely for validation. |
| Stripe Payments | WeatherTech-only owner-approved Payment Element, payment webhook, accounting reconciliation, and full-refund workflow. | Mostly Complete | WeatherTech Roofing owner/financial operations | Stripe account mapping, invoices, payments, webhook ledger, refund RPC | `components/StripeInvoicePayment.tsx`, `components/StripeInvoiceRefund.tsx`, `lib/stripe`, Stripe API routes | `/api/integrations/stripe/payment-intents`, `/api/integrations/stripe/webhook`, `/api/integrations/stripe/refunds`, `/api/integrations/stripe/readiness` | The company-isolated Stripe foundation, Payment Element, authenticated webhook processing, idempotent refund reconciliation, and controlled live WeatherTech payment/refund validation are complete. Ordinary live-write gates remain fail-closed. | Do not rebuild this foundation. IHC requires its own separately authorized Stripe account/configuration before any IHC payment capability. |
| Change Orders | Manage job change order records and statuses. | Partially Complete | Production and sales | `change_orders`, jobs, customers, estimates | `ChangeOrdersView`, repository functions | Root app `/` with `changeOrders` workspace view | Change order foundation exists. | Signature/payment linkage can be expanded later. |
| Analytics | Company-aware dashboard analytics and KPI summaries. | Partially Complete | Owner/management | Dashboard metrics, companies, jobs, estimates, invoices | `AnalyticsView`, metrics helpers | Root app `/` with `analytics` workspace view | Analytics foundation exists. | Deeper profitability and reporting are future work. |
| AI Tools | AI Command Center 3.0 operating brain for grounded priorities, advisor modes, command responses, drafts, cross-module recommendations, and approval-gated actions. | Partially Complete | Office staff and owner/admin reviewers | `CrmSnapshot`, company scope, operational modules, migration `0033_ai_tools_operating_brain.sql`, safe server AI environment variables | `AiToolsView`, `lib/crm/aiTools.ts`, `lib/crm/aiProvider.ts`, `/api/ai-tools/command` | Root app `/` with `ai` workspace view | AI Command Center 3.0 is implemented on the AI Tools 2.1 foundation with company-scoped context, prompt-safety controls, usage/cost visibility, advisor modes, grounded recommendations, and non-executing approval previews. | Do not rebuild AI Command Center 3.0. Live provider credentials or executable actions require a separately approved activation/rework sprint. |
| Weather | Weather dashboard and production guidance using existing available data. | Foundation Only | Production coordination | Weather view and job schedule context | `WeatherDashboardView` | Root app `/` with `weather` workspace view | Weather workspace exists. | Live weather API integration requires approved provider setup. |
| Notifications | Notification/reminder records and operational visibility. | Partially Complete | Office staff | `notifications`, customers/jobs/estimates context | `NotificationsView`, toast viewport for UI feedback | Root app `/` with `notifications` workspace view | Notification records and UI feedback exist. | Live email/SMS reminders require provider approval. |

## Registry Maintenance Rule

Update this registry when a sprint adds, removes, renames, or materially changes a module. Do not use this registry to authorize new work; sprint approval remains governed by [OWNER_APPROVAL.md](../project-management/OWNER_APPROVAL.md) and [CURRENT_SPRINT.md](../project-management/CURRENT_SPRINT.md).
