# Codex Browser Regression Suite

WeatherTech OS has a browser regression suite for workflows that require the signed-in Codex in-app Browser session. Routine write-capable runs use the isolated target and lifecycle in [Non-Production Regression Environment](./NON_PRODUCTION_REGRESSION_ENVIRONMENT.md).

## Entry Point

From the active WeatherTech OS checkout, print the runner command:

```bash
cd "/Users/spotty/Documents/GitHub/WeatherTech-OS"
npm run test:browser:codex
```

Run only the JavaScript printed by that command from a Codex session where the in-app Browser is open, signed in to the synthetic regression identity, and pointed at the locally served application URL. Do not reuse a command printed from `/Users/spotty/Documents/New project` or another checkout.

The npm command prints the in-app Browser entrypoint; printing it is not a test pass. A complete pass requires actual execution of every default group, nonzero assertions, console results, bounded cleanup, and zero residue.

## Mandatory Target Preflight

Before the suite creates or deletes a record, its implemented preflight must positively identify an explicitly authorized non-production Supabase target. The suite must fail closed when:

- the target identity is missing, ambiguous, or Production Supabase;
- production credentials are present without a separate purpose-built production-validation authorization;
- seed and cleanup resolve to different targets; or
- cleanup cannot be limited to IDs created by the current authorized run.

Localhost, a `TEST WTOS REGRESSION` prefix, synthetic content, or possession of a service-role credential does not authorize a database write. Never bypass the target preflight to make a browser shard run.

Local Supabase is allowed by its local hostname. The approved shared hosted target is the dedicated `WeatherTech OS Regression` project (`hygtnhmmaoboduqghhwg`). It additionally requires both runtime values below; never set them to the WeatherTech production project reference:

```text
WTOS_BROWSER_REGRESSION_REMOTE_WRITES_ENABLED=true
WTOS_BROWSER_REGRESSION_EXPECTED_PROJECT_REF=hygtnhmmaoboduqghhwg
```

The known WeatherTech production reference and the repository's currently linked Supabase reference remain permanently blocked from this ordinary harness even when those values are supplied.

Regression credentials must come from a permission-restricted file outside the checkout or protected secret storage. The harness selects the local file through the absolute-path `WTOS_BROWSER_REGRESSION_ENV_FILE` variable and rejects a group/other-readable file or one inside the repository. The verified workstation path is `/Users/spotty/.config/weathertech-os/regression.env` with mode `0600`. Export the two hosted authorization variables separately; do not also export target URL/key values or the harness will reject the ambiguous dual source. Do not modify or load production values from `.env.local` for a hosted regression run.

The local application process must also receive `NEXT_PUBLIC_DISABLE_CRM_DEMO_FALLBACK=true` and every provider/live-write gate must be false or unset. Mighty Apes endpoint coverage additionally requires a synthetic `MIGHTY_APES_YELP_WEBHOOK_SECRET` loaded only from the permission-restricted external regression environment. That value signs local requests; it is not a live-write gate, must not match the Production secret, and must never be committed or printed. Before opening the browser app, the harness verifies the raw server HTML reports the exact approved public Supabase origin, disabled demo fallback, and an aggregate disabled provider-side-effect state. It then requires the rendered page to match those same non-secret markers before authentication or database/API work.

If the approved target is unavailable, run non-writing browser smoke coverage and the automated fail-closed isolation tests. Report the write-capable browser groups as not run and state the residual coverage gap; do not point them at production.

## Sharded Runs

When browser automation is slow, use the module URL printed from the active checkout and select only the required groups. For example:

```js
var weatherTechRegression = await import("file:///Users/spotty/Documents/GitHub/WeatherTech-OS/tests/codex-browser/weathertech-os-regression.mjs?run=" + Date.now());

var smokeResult = await weatherTechRegression.runWeatherTechOsRegression({
  browser,
  nodeRepl,
  groups: ["dashboard", "themes", "layout"],
});
nodeRepl.write(weatherTechRegression.formatRegressionReport(smokeResult));
```

Write-capable CRM, job, dispatch, intake, financial, or cleanup shards remain subject to the same verified-target preflight. Run-specific disposable IDs must be retained for bounded cleanup on that same target.

Mighty Apes test deliveries use the distinct exact marker `TEST WTOS MIGHTY APES REGRESSION:`. The lead-intake group must verify audit-only `lead.test`, atomic `lead.created`, exact retry, normal Leads/Inbox visibility, and IHC exclusion. Before deleting any Browser or Mighty Apes source row, cleanup must discover the complete source-rooted automation event, execution, attempt, dynamic-audit, and engine-task graph and submit its exact IDs to `wtos_cleanup_synthetic_automation_fixture`. That service-role-only RPC re-verifies the pinned regression JWT claims, synthetic owner metadata and memberships, exact Browser or Mighty marker family, complete table-qualified source set, and same-company causation graph under relation locks. It permits immutable deletes only inside that exact transaction, returns sanitized counts, and proves zero. Ordinary cleanup then removes captured provider audit, intake, sync-log, notification, office-task, and lead IDs. Preflight and independent `verify-residue` treat every dynamic automation row or automation-linked task as release-blocking residue; seeded rule-history audits are the only ledger exception.

Lead Attribution & Marketing Accountability uses the targeted `crm-accountability` group for attribution review, explicit owner assignment, successful-human-contact evidence, linked appointment/inspection/estimate milestones, won/lost enforcement, repeat opportunity, spend entry, dashboard formulas, and company switching. Before leaving Sales Pipeline after a lost action, the harness requires the exact persisted record version, terminal lost panel, success notice, and idle accountability control; Marketing entry then permits only two bounded All-companies navigation attempts and requires the exact selected scope, active nav, foundation/workspace, and both company filter options, with structured shell/loading/error diagnostics on failure. The complete default run continues to use the 24-group set; its `sales-pipeline`, `lead-intake-workspace`, `lead-intake`, and `marketing` groups exercise the integrated accountability surfaces. Cleanup removes only captured/current-run accountability events, accountability rows, operation receipts, spend, campaigns, repeat opportunities, and their exact linked fixtures, then proves zero residue.

Inspection record management opens the exact cancel confirmation and permits at most two dialog-scoped visible-coordinate `Confirm cancel` activations. Each activation pre-reads the exact inspection identity and can retry only while that row remains non-canceled, the exact dialog/button is present and enabled, and no error notice exists; terminal diagnostics preserve the database ID/status, dialog/button state, Saving state, error, and notice before the existing canceled notice/filter/restore assertions continue.

Secure Company-Scoped Job Photos uses the targeted `job-photos` group for an authenticated private upload, company-filtered relation options, short-lived signed preview/copy/open, reload persistence, URL-free metadata readback, gallery company isolation, Customer 360 linkage, independent-tab waiting, and same-token reload/internal-navigation cleanup. Before upload, the harness verifies the exact seeded customer and company through REST, boundedly waits for both exact WeatherTech job/customer options, permits at most one hard snapshot refresh if those options are initially stale, proves both relations absent under IHC, and re-proves both present under WeatherTech before selecting them. Preview validation requires a rendered image backed by the exact tokenized company path, an HTTP 200 `image/png` response, and bytes identical to the fixture; copy uses the supported browser clipboard control, while open verifies a real newly created signed-photo tab, observes its exact signed URL through the Browser tab getter without attaching a raw-image navigation waiter, then closes it with at most three exact controlled-ID attempts separated by bounded 250 ms settling before proving both its controlled and provider identities are absent. A raced controlled-tab reacquisition counts as closed only when a fresh inventory proves that exact ID is gone, and a missing provider identity fails closed only after controlled cleanup has run. The harness never reads or writes browser storage: after the real UI upload persists, it reads that upload's exact committed operation service-side to obtain the non-PII recovery token, fully navigates the primary tab to a unique inert same-origin Next 404 and proves the CRM shell is absent before seeding, then seeds one interrupted operation with the same token and proves a second tab remains in the visible `waiting` state without changing the reserved row, token, object, or metadata. After that tab closes, navigating the primary tab back to the app and later using internal navigation must each converge through the existing recovery workflow with zero console errors or warnings. The complete default run keeps the same 24-group inventory and reaches this coverage through `field-operations`; its existing `inspections` and `customer-portal` groups additionally exercise their affected upload/visibility surfaces. File selection follows the Browser chooser boundary only: each upload, including the invalid-MIME refusal case, first proves one connected, visible, enabled single-file input, centers it, permits at most three fresh chooser/click attempts for the exact known transient errors, fully settles both promises, and verifies one selected file with the expected basename; direct input-file mutation and direct one-off chooser waits are forbidden. Its exact Inspection photo submit is centered and transient-click retried. Field Operations first proves its exact persisted job and inspection, permits only two reload/open/select-all settlement attempts, and then selects the exact company-scoped job card before requiring its detail and forms; terminal failures include structured database and UI state. After Work Started persists, the checklist path waits for the exact success notice and idle status control, re-resolves the seeded task, and retries only until that same task UUID is done with its structured completion description. Its field upload selects the canonical `During-work photos` option and requires that exact category in persisted metadata. Field issue submission is retried only until its run-specific note persists; material submission retries its exact control only while neither its run-specific material row nor structured office note exists, stops clicking as soon as either durable effect appears, then requires both exact records. All persistence-aware paths fail immediately on a visible application error. Generic submit fallback chains stop after the first successful activation so one logical action does not trigger later fallback clicks. Job-photo fixtures reserve a durable lease-bound operation, upload the exact private object, and register it through `wtos_register_job_photo`; interrupted fixtures verify claim/remove/confirm recovery, while cleanup captures exact `file_path` values, removes only those Storage objects before their exact metadata and operation rows, and proves zero object and row residue.

Native proposal signing uses the targeted `proposal-signing` group and does not change the established 24-group default inventory. It seeds exact synthetic WeatherTech required-deposit and IHC no-deposit proposals, proves a draft estimate cannot finalize, then approves and finalizes the immutable customer artifact through the real owner API. The synthetic delivery boundary is available only from a loopback app connected to the fixed regression project with every provider gate disabled; it persists only the token digest and returns the raw fragment link once in a no-store response without calling Gmail. The customer pass covers deliberate fragment exchange, immediate fragment removal, mobile and desktop layout, private source-PDF and receipt streaming with byte digests, frozen options and totals, exact canonical intended-signer enforcement, all acknowledgements, pre-sign one-time-link and action replay refusal, direct/no-session denial, cross-company preparation denial, expired and revoked denial, persistent rate limiting, required-deposit conversion refusal, same-company wrong-customer posted-payment refusal, exact correctly scoped posted-deposit success, no-deposit conversion, and preservation of the approved estimate state. It also deliberately drops the first successful exchange response, retries with the same browser-memory-only 256-bit exchange key to recover the single already committed session, and proves that a different key cannot mint another unsigned or action-capable session or enter persisted evidence; only after exact acceptance may reopening the exact still-valid raw invitation mint a terminal read-only signed session, bounded by the original request deadline, for review and signed-receipt download. The page, finalized PDF, and receipt all disclose the frozen base subtotal, discount, tax, fees, base total, selected upgrades, accepted total, price-effect type, replacement basis/net adjustment, deposit type/value, required deposit, and remaining balance. The deterministic signed receipt is a combined completed customer copy: it contains the full frozen customer-visible proposal sections, line items, every frozen option and its selection state, exact pricing, deposit and balance, terms, transaction-specific electronic-records disclosure, typed-signature certificate, evidence identifiers, and all binding digests; Browser validation checks both its registered byte digest and those content boundaries. After each customer signature, the same tab returns to the exact company-scoped owner estimate URL and proves the immutable proposal, signer evidence, accepted total, browser-history restoration, and hard-refresh restoration. It uses owner receipt recovery only if the registered receipt is missing, creates and opens the exact required-deposit invoice through the owner UI, posts only synthetic offline regression payments service-side, proves the wrong customer cannot satisfy either the UI or database gate, and performs the sold-job conversion through the owner confirmation UI only after the exact correctly scoped payment before proving exact job focus and refresh restoration; an idempotent RPC readback verifies the resulting totals and deposit evidence. Cleanup first removes and verifies absence of the exact private Storage paths, then calls the service-role-only synthetic cleanup RPC with the complete captured graph; that RPC independently verifies the marked regression owner, company, revision, IDs, and zero Storage residue before its transaction-local append-only bypass can delete anything. The shard fails unless both Storage and database residue are exactly zero. No real email, payment provider, or customer contact is permitted.

The same shard preserves customer-visible line-item descriptions and full option scope, warranty, notes, quantity, unit, price, selection state, non-additive pricing effect, and deposit configuration on the page and completed receipt. Its electronic-records checks preserve the exact versioned disclosure covering record access and retention, pre-signature withdrawal, technical requirements, paper-copy requests, and the normal electronic workflow. Its synthetic identities exercise exact accented Latin and Cyrillic text through the pinned deterministic PDF renderer; unsupported glyphs fail before finalization instead of being transliterated. Database-negative checks require `P0001` when a null-linked generic signature is inserted or updated onto a native finalized proposal document or when a non-electronic acceptance targets a native-finalized revision, while proving an unrelated generic signature remains allowed and is removed by exact-ID cleanup.

A shard is diagnostic evidence only. It must never be reported as a complete browser-regression pass. Full-run logic must reject an empty, unknown, duplicate, or incomplete group list and must report the expected group count and nonzero assertion count.

## CI Boundary

GitHub's repository-validation job runs repository tests, type-check, lint, build, dependency audit, and whitespace validation without any database credentials. Its push/manual-only lifecycle job uses protected secrets to verify the exact isolated target, bootstrap marked synthetic identities, retain the exact-ID notification lifecycle, prove private job-photo upload/read/isolation/idempotency/reservation-cancellation and recovery-lease invariants, clean exact Storage objects before metadata and durable operation rows, and verify zero residue with provider gates false. The Codex Browser harness uses in-app Browser capabilities that are not present on a GitHub-hosted runner; therefore neither job claims a browser pass.

Do not create a conditional browser job that turns green after skipping for missing credentials or browser capability. A future compatible runner must execute the full expected group set, fail on zero assertions or incomplete coverage, and prove cleanup plus zero residue.

## Production Validation Boundary

A narrowly authorized production validation, such as a single owner-approved Stripe transaction, is a separate workflow. It must name the exact purpose and allowed mutation, enforce its own approval and company-isolation controls, and must not enable the ordinary regression harness to seed or clean production data.

## Latest Verified Accountability Run

- Implementation commit: `ba816c2bad315f7ef85051bb3e247f2f965f50b6`.
- Targeted accountability run `20260816165039517`: `3/3` assertions passed.
- Post-hardening Sales run `20260816171149423`: `1/1` assertion passed.
- Complete run `20260816171236859`: `24/24` expected groups and `30/30` assertions passed.
- All three runs reported zero browser-console errors, zero warnings, bounded cleanup, and zero residue on the approved regression project.
- Production was limited to read-only release verification. The ordinary harness did not seed, mutate, or clean Production.
